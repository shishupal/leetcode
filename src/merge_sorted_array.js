/**
 * First array has n positive numbers, 
 * and they are sorted in the non-descending order.
 * 
 * Second array has 2n numbers:
 *  first n are also positive and sorted in the same way 
 *  but the last n are all zeroes.
 * 
 * Merge the first array into the second and return the latter.
 *  You should get 2n positive integers sorted in the non-descending order.
 * 
 * 
{
    "first": [1, 3, 5],
    "second": [2, 4, 6, 0, 0, 0]
    }

    [1, 2, 3, 4, 5, 6]
 * 
 * **/

    function merge_one_into_another(arr1, arr2) {
        
        
        let arr = [arr1, arr2];

        if(arr1.length > arr2.length){
            
        }
    }

    console.log(merge_one_into_another([11, 4],[3, 11, 0, 0]));


 

